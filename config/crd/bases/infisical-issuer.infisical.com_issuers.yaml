---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: issuers.infisical-issuer.infisical.com
spec:
  group: infisical-issuer.infisical.com
  names:
    kind: Issuer
    listKind: IssuerList
    plural: issuers
    singular: issuer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Issuer is the Schema for the issuers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IssuerSpec defines the desired state of an Issuer or ClusterIssuer.
            properties:
              application:
                description: Application is the name of the Infisical PKI Application
                  to issue through.
                type: string
              authentication:
                description: Authentication selects and configures the Machine Identity
                  auth method.
                properties:
                  kubernetes:
                    description: Kubernetes holds the configuration for Kubernetes
                      Auth. Required when method is "kubernetes".
                    properties:
                      identityIdRef:
                        description: IdentityIDRef references the Secret key holding
                          the Machine Identity ID.
                        properties:
                          key:
                            description: Key within the Secret that holds the value.
                            type: string
                          name:
                            description: Name of the Kubernetes Secret.
                            type: string
                          namespace:
                            description: Namespace of the Kubernetes Secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      serviceAccountRef:
                        description: ServiceAccountRef is the service account whose
                          token the issuer presents to Infisical.
                        properties:
                          name:
                            type: string
                          namespace:
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                      serviceAccountTokenAudiences:
                        description: ServiceAccountTokenAudiences are optional custom
                          audiences for the minted token.
                        items:
                          type: string
                        type: array
                    required:
                    - identityIdRef
                    - serviceAccountRef
                    type: object
                  method:
                    description: Method selects the authentication method. One of
                      "universal" or "kubernetes".
                    enum:
                    - universal
                    - kubernetes
                    type: string
                  universal:
                    description: Universal holds the configuration for Universal Auth.
                      Required when method is "universal".
                    properties:
                      clientIdRef:
                        description: ClientIDRef references the Secret key holding
                          the Machine Identity client ID.
                        properties:
                          key:
                            description: Key within the Secret that holds the value.
                            type: string
                          name:
                            description: Name of the Kubernetes Secret.
                            type: string
                          namespace:
                            description: Namespace of the Kubernetes Secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      clientSecretRef:
                        description: ClientSecretRef references the Secret key holding
                          the Machine Identity client secret.
                        properties:
                          key:
                            description: Key within the Secret that holds the value.
                            type: string
                          name:
                            description: Name of the Kubernetes Secret.
                            type: string
                          namespace:
                            description: Namespace of the Kubernetes Secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                    required:
                    - clientIdRef
                    - clientSecretRef
                    type: object
                required:
                - method
                type: object
              profile:
                description: Profile is the name of the Certificate Profile to issue
                  with.
                type: string
              tls:
                description: |-
                  TLS optionally configures a custom CA certificate for verifying a self-hosted
                  Infisical instance. If omitted, the system trust store is used.
                properties:
                  caCertificate:
                    description: |-
                      CACertificate references a Secret key holding a PEM CA certificate used to verify
                      the connection to a self-hosted Infisical.
                    properties:
                      key:
                        description: Key within the Secret that holds the value.
                        type: string
                      name:
                        description: Name of the Kubernetes Secret.
                        type: string
                      namespace:
                        description: Namespace of the Kubernetes Secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                required:
                - caCertificate
                type: object
              url:
                description: URL is the base URL of the Infisical instance, e.g. "https://app.infisical.com".
                type: string
            required:
            - application
            - authentication
            - profile
            - url
            type: object
          status:
            description: IssuerStatus defines the observed state of Issuer
            properties:
              conditions:
                description: Conditions indicate the status of the Issuer. The known
                  condition type is `Ready`.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
